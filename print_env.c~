#include "shell.h"
/**
 * print_env - Print environment variables
 */
void print_env(void)
{
	char *env_copy;
	char **env;

	for (env = environ; *env != NULL; env++)
	{
		env_copy = *env;
		shell_print(env_copy);
	}
}
